extends base

block append scripts
  script(src="js/lib/ui-bootstrap-datepicker-tpls-0.10.0.min.js")
  script(src="js/cleanlifts.js")

block body
  nav
    .content
      .left
        a.h2-font-size(ui-sref="user.index")!="UNDEADLIFTS"
      .right
        a.h3-font-size(ng-if="user", ng-click="logout()")!="Logout"
  section(ui-view="")
    .heading
      .tac
        .ib.tal.p1em.w100.mxw20em
          h1
            | Loading...
